[package]
name = "makepad-example-hand-landmarks"
version = "1.0.0"
edition = "2021"

[features]
# The demo GUI app (src/app.rs, which expands `app_main!`). On by default so the
# example's own binary builds; library consumers can disable it.
default = ["app"]
app = []

[dependencies]
makepad-widgets = { path = "../../widgets", version = "2.0.0" }
makepad-ggml = { path = "../../libs/ggml" }
makepad-nn-graph = { path = "../../libs/nn_graph" }
makepad-zune-core = { path = "../../libs/zune/zune-core" }
makepad-zune-jpeg = { path = "../../libs/zune/zune-jpeg" }
makepad-zune-png = { path = "../../libs/zune/zune-png" }

[[test]]
name = "real_model"
path = "tests/real_model.rs"
